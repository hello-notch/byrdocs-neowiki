---
时间: 2024-2025学年第二学期
科目: 离散数学
阶段: 期中
类型: 本科
来源: 93c0036c6d70004706d7e5974867d5ee
答案完成度: 完整可靠
学院:
- 人工智能学院
---

## 一、选择正确答案（每题5分，共10题）

1. 以下命题为真的是 <Slot />。

<Choices>
    - $a\in\{\{a\}\}$
    + $\{x\}\subset\{x\}-\{\{x\}\}$
    - $A\oplus A=A$
    - $A\cap B=B\Leftrightarrow A=P(B)$
</Choices>

2. 在一个班级里，有 30 名学生。已知其中有 18 名学生喜欢数学，12 名学生喜欢英语，7 名学生同时喜欢数学和英语。请问，喜欢数学或英语的学生一共有多少名？

<Choices>
    + 23
    - 25
    - 30
    - 18
</Choices>

3. 集合 $A=\{a,\{b,c\},\{c,d\}\}$，则 $\cup A=$ <Slot />.

<Choices>
    - $\{a,b,c,d\}$
    - $\{a,\{b,c,d\}\}$
    - $a\cup\{b,c,d\}$
    + $\{b,c,d\}$
</Choices>

4. 下列公式中哪个是矛盾式？<Slot />

<Choices>
    - $\forall x(F(x)\rightarrow G(x,y))$
    - $\exists x(F(x)\land G(x,y))$
    - $\forall xF(x)\rightarrow(\exists x\exists yG(x,y)\rightarrow\forall xF(x))$
    + $\lnot(\forall xF(x)\rightarrow\exists yG(y))\land\exists yG(y)$
</Choices>

5. $\exists xF(x)\rightarrow\forall xG(x)$ 的前束范式是？<Slot />

<Choices>
    - $\forall x(\lnot F(x)\lor G(x))$
    - $\exists x(F(x)\lor G(x))$
    + $\forall x\forall y(F(x)\rightarrow G(y))$
    - $\exists x(G(x)\land F(x))$
</Choices>

6. 对以下公式进行消解，无法得到的消解式是 <Slot />。

$$
\lnot p\land(p\lor q)\land(\lnot p\lor\lnot q)\land(\lnot q\lor r)\land(p\lor\lnot r)
$$

<Choices>
    - $\lnot r$
    - $q$
    - $r$
    - $\lnot q$
</Choices>

7. 下列哪个联结词集不完备？<Slot />

<Choices>
    + $\{\land,\lor\}$
    - $\{\land,\lnot\}$
    - $\{\lor,\lnot\}$
    - $\{\land,\rightarrow\}$
</Choices>

8. 命题公式 $p\land(((p\lor q)\land\lnot p)\rightarrow q)$ 的类型是 <Slot />。

<Choices>
    - 重言式
    - 矛盾式
    + 可满足式
    - 取决于具体赋值
</Choices>

9. 使推理 $\{p,q,\lnot(p\land q)\lor r\}\vdash r$ 成立的直接关键推理定律是 <Slot />。

<Choices>
    - 假言推理
    + 析取三段论
    - 假言三段论
    - 破坏性二难
</Choices>

10. 给定集合 $A$ 和 $B$，下式不是命题的有 <Slot />。

<Choices>
    - $\left|A\right|>\left|B\right|$
    - $A\subset B$
    - $A\cap B=\varnothing$
    + $A\cap B$
</Choices>

## 二、选择正确答案（每题10分，共5题）

11. 命题公式 $(p\land q)\lor(\lnot p\land r)\lor(q\land\lnot r)$ 的主合取范式是 <Slot />。

<Choices>
    - $M_1\lor M_3\lor M_5\lor M_6$
    - $M_0\lor M_1\lor M_2\lor M_6$
    - $M_2\lor M_4\lor M_5$
    - $M_3\lor M_5\lor M_7$
</Choices>

<Solution>
    本题为错题，正确答案为 $M_4\land M_5\land M_0$
</Solution>

12. 考虑命题公式 $p\land q\rightarrow r$。下列哪一个命题公式与其不等值？<Slot />

<Choices>
    - $p\rightarrow(q\rightarrow r)$
    - $(p\land q)\rightarrow r$
    - $\lnot r\rightarrow(\lnot p\lor\lnot q)$
    + $r\rightarrow(p\lor q)$
</Choices>

13. $((X\cap Y)\cup(Y\cap Z))-((X\cap(Y\cup Z))\cap Y)=$ <Slot />

<Choices>
    - $X-Z$
    - $(X\cap Y)-Z$
    + $(Y\cap Z)-X$
    - $(X\cap Y)\cup Z$
</Choices>

14. 下列哪个推理正确？<Slot />

<Choices>
    + $\{p\rightarrow q,p\}\vdash p$
    - $\{p\rightarrow q,\lnot q\}\vdash p$
    - $\{p,p\rightarrow q\}\vdash\lnot q$
    - $\{p,q\rightarrow p\}\vdash q$
</Choices>

15. 命题公式 $\lnot(p\rightarrow q)\land q\lor r$ 的主析取范式是 <Slot />。

<Choices>
    - $m_0\lor m_2\lor m_4\lor m_6$
    + $m_1\lor m_3\lor m_5\lor m_7$
    - $m_0\lor m_2\lor m_4\lor m_6\lor m_3$
    - $m_2\lor m_4\lor m_6$
</Choices>
